import React from 'react';
import { View, Text } from 'react-native';

export const RestaurantsDetail = (props) => {
    return(
        <View>
            <Text>RestaurantsDetail comp</Text>
        </View>
    )
}